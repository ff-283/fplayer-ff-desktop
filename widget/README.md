桌面端 UI 层（Qt Widget）。

业务与交互说明见仓库根目录 `doc/`（例如 **`doc/推拉流-使用说明.md`**、**`doc/推拉流-开发文档.md`**）。组合模式下底部播放键与当前选中素材的同步、屏幕采集单路激活、预览绑定顺序等见 **`doc/推拉流-开发文档.md`** 与 **`widget/src/capturewindow.cpp`**。

这里暂时将部分核心逻辑与 UI 写在一起，后续可再拆分；例如摄像头列表、设备枚举等仍可从本模块入口查找。