# 马尾辫优化清单

> 2026-07-02 代码审查产物。原则：删 > 改 > 加。先修 Bug，再砍死代码，最后消除重复。

---

## 🔴 高优先级 — Bug / 逻辑错误

- [x] **#1** [streamffmpeg.cpp:L228-233](../backend/stream_ffmpeg/src/streamffmpeg.cpp#L228) — `FilePreview` 路由到了 `pushScreenPreviewLoop`（屏幕预览函数），而非文件预览循环。→ 改为 `remuxLoop(route.spec, outputUrl, "flv")`。`// ponytail: no dedicated file preview loop yet, remux does the job`
- [x] **#2** [service.cpp:L129-135](../service/src/service.cpp#L129) — `selectCamera()` 先 `m_cameraIndex = -1` 再检查成功。失败时丢失之前选中的相机索引。→ 只在成功时赋值。
- [x] **#3** [3rd.cmake:L2](../cmake/3rd.cmake#L2) — `SPDLOG_BUILD_SHARED ON` 但中文注释说"生成静态库"。→ 删掉错误注释，代码 (`ON` = shared) 保持不变。
- [x] **#4** [utils.cmake:L28-30](../cmake/utils.cmake#L28) — `if (NOT ${is_shared})` 逻辑正确（项目强制 shared），仅错误信息令人困惑。→ 改善错误信息为"仅支持共享库构建 (is_shared=ON)，不支持静态库"。

---

## 🟡 中优先级 — 死代码 / 可删

- [~] **#5** [imagepoolservice.h](../service/include/fplayer/service/imagepoolservice.h) — `SortMode` 枚举（6 个值）→ 初次删除后发现 `imagepoolsidebar.cpp` 实际使用，已恢复。**保留。**
- [x] **#6** [imagepoolservice.h](../service/include/fplayer/service/imagepoolservice.h) — `kThumbnailLoadBatchSize = 20` 无人引用。→ 已删。
- [x] **#7** [cameraffmpeg.cpp](../backend/media_ffmpeg/src/cameraffmpeg.cpp) — `fGLWieget`（拼写错误）存了从不读。→ 已删声明和赋值。
- [x] **#8** [fglwidget.cpp](../common/src/fglwidget.cpp) — `canUseUnpackRowLength()` 定义但从不调用，22 行死代码。→ 已删。
- [x] **#9** [fglwidget.cpp](../common/src/fglwidget.cpp) — `#if 0` + `(void)firstFrame`/`(void)formatChanged` dead block。→ 已删 block 和未使用变量。
- [x] **#10** [net_qt6/](../backend/net_qt6/) — `StreamQt6` 空壳。→ `FPLAYER_BUILD_NET_QT6` 默认改为 `OFF`。
- [x] **#11** [test_ffmpeg/](../backend/test_ffmpeg/) — 孤立测试目录，从不构建。→ 已删整个目录。
- [x] **#12** [CPM.cmake](../cmake/CPM.cmake) — CPM_DOWNLOAD 变量和 `include(${CPM_DOWNLOAD_LOCATION})` 永不可达。→ 已删。
- [x] **#13** [utils.cmake](../cmake/utils.cmake) — `install_qt_libs()` 被完整注释掉 90 行。→ 已删。
- [x] **#14** [DESIGN.md](../DESIGN.md) — 全文件是 Anthropic Claude 产品设计。→ 已删。
- [x] **#15** [scripts/](../scripts/) — `powershell脚本启动参数 copy.txt` Windows 备份副本。→ 已删。
- [x] **#16** [capturewindow.cpp](../widget/src/capturewindow.cpp) — 4 个三元表达式两边值相同。→ 删三元，直接返回。
- [x] **#17** [qtloggeradapter.h](../common/include/fplayer/common/qtloggeradapter/qtloggeradapter.h) — `static thread_local` 在头文件，每个 TU 独立副本。→ `extern thread_local` + `.cpp` 定义。

---

## 🟢 低优先级 — 重复代码 / 可简化

- [ ] **#18** [capturewindow.cpp](../widget/src/capturewindow.cpp) — 6746 行上帝类，管理相机/文件/屏幕/合成/推拉流/录制/截图/设置/托盘/AI。应拆 4-5 个类。
- [ ] **#19** [streamffmpeg.cpp](../backend/stream_ffmpeg/src/streamffmpeg.cpp) — 音频 pipeline 初始化在 3 个 push 函数中逐字重复，每块 ~300-500 行。提取为 `setupAudioPipeline()`。
- [ ] **#20** [CameraFrameBus + ScreenFrameBus](../common/) — 两个全局单例 95% 代码相同。ScreenFrameBus 是 CameraFrameBus 超集。用模板或继承消除。
- [x] **#21** [loggerCallback 两份](../backend/media_ffmpeg/src/) — `cameraffmpeg.cpp` 和 `camerainfofetcher.cpp` 中各一份 49 行完全相同的 FFmpeg 日志回调。→ 提取到共享 [ffmpeg_logger.h](../backend/media_ffmpeg/include/fplayer/backend/media_ffmpeg/ffmpeg_logger.h)，两个源文件改为 include。
- [x] **#22** [capturewindow.cpp](../widget/src/capturewindow.cpp) — FPS 候选列表 `{15,24,25,...}` 和过滤逻辑在两处重复。→ 提取为 `kFpsCandidates` 常量。
- [ ] **#23** [capturewindow.cpp](../widget/src/capturewindow.cpp) — HTTP 请求+超时模式在 `requestServiceStreamStart()` 和 `requestServiceStreamStatus()` 中重复。
- [ ] **#24** [capturewindow.cpp](../widget/src/capturewindow.cpp) — IP 过滤逻辑（IPv4 + 非本地 + 非 169.254）在 `selectLanHostForPublish` 和 `collectLanIpv4List` 中重复。
- [x] **#25** [capturewindow.cpp](../widget/src/capturewindow.cpp) — `saveCapturePreferences() + applyTheme()` 被成对调用 7 次。→ 包装为 `saveAndApplyTheme()`。
- [ ] **#26** [systemsettingsrepository.cpp](../service/src/systemsettingsrepository.cpp) — ~30 字段手动 `YamlTool::getDef`/`set`。用宏消除。
- [x] **#27** [imagepoolservice.cpp](../service/src/imagepoolservice.cpp) — for 循环逐个 append = 直接赋值。→ `nameFilters = supportedExtensions()`。
- [ ] **#28** [service.h](../service/include/fplayer/service/service.h) — 60+ 方法上帝类 + include guard 拼写 `DESKETOP` → `DESKTOP`。
- [x] **#29** [service.cpp](../service/src/service.cpp) — 未使用的 include：`QVideoWidget`、`QStandardPaths`。→ 已删。
- [x] **#30** [aiservice.cpp](../service/src/aiservice.cpp) — 未使用的 include：`QFile`。→ 已删。
- [x] **#31** [maplist.hpp](../common/include/fplayer/common/maplist/maplist.hpp) — `mapList<T>` = `std::transform`。→ 1 处调用改为简单 for 循环，文件已删。
- [x] **#32** [capturewindow.cpp](../widget/src/capturewindow.cpp) — `clampRgb(v)` = `qBound(0, v, 255)`。→ 删自定义函数，3 处调用改为 `qBound`。
- [ ] **#33** [5个 export.h](../backend/) — 5 个结构完全相同仅宏名不同。共享一份模板。
- [x] **#34** [app/CMakeLists.txt](../app/CMakeLists.txt) — GLOB 了不存在的 `*.ui` 和 `res/*.qrc`。→ 删了。
- [x] **#35** [app/CMakeLists.txt](../app/CMakeLists.txt) — 可执行目标上设 `INCLUDES DESTINATION` 无意义。→ 删了，只留下 `RUNTIME DESTINATION`。
- [ ] **#36** [vc_redist.x64.exe](../cmake/vc_redist.x64.exe) — 25MB 二进制在 git 中。CMake 已有下载逻辑。移到 disk/ 或 LFS。
- [ ] **#37** [service/README.md](../service/README.md) — 150+ 行 ChatGPT 对话记录。替换为简短说明。
- [ ] **#38** [IFVideoView](../api/include/fplayer/api/media/ifvideoview.h) — 接口仅有一个实现 (FVideoView)。合并或删接口。
- [ ] **#39** [aichatdialog.cpp:L337](../widget/src/aichatdialog.cpp#L337) — `setInputEnabled(bool)` 忽略参数，输入框永远 enabled。
- [ ] **#40** [capturewindow.cpp:L254-303](../widget/src/capturewindow.cpp#L254) — `i420ToImage()` 只用 BT.601，GL 着色器已支持 BT.709 切换。HD 内容颜色偏。
- [ ] **#41** [capturewindow.cpp:L5567](../widget/src/capturewindow.cpp#L5567) — 移除合成源时无条件调 `cameraPause()`，对 File/Screen 源无效但令人困惑。

---

## 统计

| 级别 | 数量 |
|------|------|
| 🔴 Bug/逻辑错误 | 4 |
| 🟡 死代码/可删 | 13 |
| 🟢 重复/可简化 | 24 |
| **合计** | **41** |
