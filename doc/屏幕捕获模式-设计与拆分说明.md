# 屏幕捕获模式设计与实现说明

## 1. 文档目的

本文档说明当前“屏幕捕获模式”的实现架构、默认后端选择、能力边界与构建开关，作为后续维护与扩展（窗口捕获、区域捕获、录制）的依据。

---

## 2. 当前结论（2026-04）

- 屏幕采集能力统一走 `IScreenCapture` 抽象。
- Windows 默认屏幕采集后端为 **DXGI Desktop Duplication**（`MediaBackendType::Dxgi`）。
- FFmpeg `gdigrab` 保留为可回退后端（或在非 DXGI 条件下使用）。
- UI 仅做交互展示，采集逻辑全部在 backend/service/runtime 链路。
- 当推流场景使用屏幕模式且当前后端为 DXGI 时，视频链路优先复用 DXGI 预览帧（不再额外开启 `gdigrab` 采集）。
- 针对 HDR 显示器新增稳定性策略：检测到 HDR 后可提示用户切换到 FFmpeg 后端。

---

## 3. 分层设计

### 3.1 API 层

- `api/include/fplayer/api/media/iscreencapture.h`
- `api/src/iscreencapture.cpp`

核心定义：

- `ScreenDescription`：屏幕元数据（名称、主副屏、像素分辨率、坐标）
- `IScreenCapture`：统一接口（枚举/选择/绑定预览/启停/鼠标/帧率）

### 3.2 后端实现层

- Qt 实现
  - `backend/media_qt6/include/fplayer/backend/media_qt6/screencaptureqt6.h`
  - `backend/media_qt6/src/screencaptureqt6.cpp`

- FFmpeg 实现（gdigrab）
  - `backend/media_ffmpeg/include/fplayer/backend/media_ffmpeg/screencaptureffmpeg.h`
  - `backend/media_ffmpeg/src/screencaptureffmpeg.cpp`

- DXGI 实现（Desktop Duplication）
  - `backend/desktopcapture_dxgi/include/fplayer/backend/desktopcapture_dxgi/screencapturedxgi.h`
  - `backend/desktopcapture_dxgi/src/screencapturedxgi.cpp`
  - `backend/desktopcapture_dxgi/CMakeLists.txt`

### 3.3 Runtime 层

- `runtime/src/runtime.cpp` 按 `MediaBackendType` 创建具体实现。
- `MediaBackendType::Dxgi` 在 Windows + 启用 DXGI 模块时返回 `ScreenCaptureDxgi`。
- 若未构建 DXGI 模块，`Dxgi` 分支回退到 `ScreenCaptureFFmpeg`。

### 3.4 Service 层

- `service/include/fplayer/service/service.h`
- `service/src/service.cpp`

提供统一业务入口：

- `initScreenCapture(...)`
- `bindScreenPreview(...)`
- `getScreenList()` / `selectScreen(...)`
- `screenSetActive(...)` / `screenIsActive()`
- `screenSetCursorCaptureEnabled(...)` / `screenCanControlCursorCapture()`
- `screenSetFrameRate(...)` / `screenCanControlFrameRate()`
- `streamStartPushByScene(...)` 在屏幕场景下根据当前屏幕后端自动选择：
  - `__screen_preview__`（DXGI 预览帧推流）
  - `__screen_capture__`（gdigrab 回退推流）

### 3.5 Widget 层

- `widget/src/capturewindow.cpp`

职责：

- 模式切换
- 控件显隐与状态展示
- 调用 Service，不直接接触后端细节

---

## 4. 运行时流程（屏幕模式）

### 4.1 进入屏幕模式

1. `CaptureWindow` 切换到 `Screen`
2. 选择屏幕采集后端（Windows 默认 DXGI）
3. `Service::bindScreenPreview()` 绑定 `FVideoView`
4. `Service::getScreenList()` 刷新下拉
5. `Service::selectScreen(index)` 选择目标屏幕
6. `Service::screenSetActive(true)` 开始采集

### 4.2 切屏

1. 下拉变化触发 `selectScreen(index)`
2. 后端停止当前采集并重建对应输出/输入对象
3. 持续输出到同一预览控件
4. 若当前后端为 DXGI 且检测到 HDR，可弹窗提示是否切换 FFmpeg 后端

### 4.3 播放/暂停按钮映射

- 屏幕模式下映射到 `screenSetActive(true/false)`。

### 4.4 屏幕推流链路（新增）

1. `ScreenCaptureDxgi` 捕获到帧后，除投递预览外同时发布到 `ScreenFrameBus`
2. `Service::streamStartPushByScene(PushScene::Screen, ...)` 识别到 `MediaBackendType::Dxgi`
3. `StreamFFmpeg` 进入 `pushScreenPreviewLoop(...)`，从 `ScreenFrameBus` 取 YUV 帧
4. FFmpeg 编码并复用输出到 RTMP/RTSP/SRT（支持自动选择 NVENC/AMF/CPU）

该链路减少了重复屏幕采集开销，并显著降低鼠标指针闪烁风险。

### 4.5 屏幕推流编码器选择（新增）

- UI 提供 `videoEncoder` 参数：`auto / cpu / nvenc / amf`
- `auto` 策略：`h264_nvenc -> h264_amf -> libx264/H264/MPEG4`
- 推流日志会打印实际命中编码器（例如 `编码器=h264_amf（硬件）`）
- 若用户强制选择 `nvenc/amf` 但运行环境不可用，会明确返回“编码器不可用”错误

---

## 5. DXGI 后端实现说明

### 5.1 核心流程

`ScreenCaptureDxgi` 采用：

- `IDXGIOutputDuplication::AcquireNextFrame()` 获取桌面帧
- D3D11 staging texture 映射到 CPU
- BGRA 数据通过 `libswscale` 转为 `YUV420P`
- `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 投递给 `FGLWidget::updateYUVFrame`

### 5.2 多显示器识别

- 当前策略为“DXGI 优先”：
  - 优先使用 `IDXGIOutput::GetDesc` 的原生 `DesktopCoordinates` 枚举屏幕坐标与分辨率
  - 若 DXGI 枚举失败，回退到 Qt `QGuiApplication::screens()` + DPR 推导
- 该策略用于避免 Qt 几何推导在高 DPI 下出现 1px 偏差（如 UI 显示 2561x1601，而实际采集 2560x1600）。

### 5.3 帧率支持

- 支持 `setFrameRate(int fps)` 动态设置采样节奏（循环内按 `1000/fps` 控制）。
- UI 侧沿用动态帧率下拉与推荐策略。

### 5.4 鼠标指针支持

- 支持开关控制是否在帧上叠加鼠标指针。
- 叠加方式为 `GetCursorInfo` + `DrawIconEx` 绘制到 BGRA 帧后再转 YUV。
- 相比 `gdigrab + CAPTUREBLT` 路径，通常可显著减少“系统光标全局闪烁”。

### 5.5 HDR / 高级色彩兼容（新增）

- 现场问题：部分机器在 DXGI 下会交替产出两类帧格式：
  - `B8G8R8A8_UNORM`（可正常处理）
  - `R16G16B16A16_FLOAT`（HDR/高级色彩场景）
- 旧行为：把 `R16G16B16A16_FLOAT` 视为采集故障，触发 duplication 重建，导致预览扭曲/抖动与推流不稳定。
- 新行为：
  1. 对不支持格式做“跳过该帧”而非“重建设备”
  2. 保留支持格式帧继续渲染
  3. 日志按时间窗口限频输出，避免逐帧刷屏
  4. 在 UI 侧检测 HDR 并弹窗提示用户可切到 FFmpeg 后端

### 5.6 日志与诊断（新增）

- DXGI 路径新增关键诊断字段：
  - 采集帧尺寸
  - `DXGI_FORMAT`
  - 每像素字节数
  - 不支持格式跳过计数
- 日志限频策略：
  - `frame meta`：约 5 秒一条（或首次/格式变化）
  - `skip unsupported format`：约 3 秒一条（附带窗口累计跳过数）

---

## 6. 构建与启用

### 6.1 CMake 开关

文件：`backend/CMakeLists.txt`

- `FPLAYER_BUILD_MEDIA_DXGI`（默认 `ON`，且仅 `WIN32` 时生效）

### 6.2 关联模块

- `backend/desktopcapture_dxgi/CMakeLists.txt`
  - 链接 `d3d11`, `dxgi`
  - 链接 `FPlayer_Common`（用于 `FGLWidget`）
  - 链接 `swscale`, `avutil`

- `runtime/CMakeLists.txt`
  - 启用 DXGI 时链接 `FPlayer_Backend_Desktop_Capture_Dxgi`
  - 导出编译宏 `FPLAYER_WITH_SCREEN_DXGI=1`

### 6.3 默认行为

- `CaptureWindow` 在 `_WIN32 + FPLAYER_WITH_SCREEN_DXGI` 条件下默认选择 `MediaBackendType::Dxgi`。
- 否则回退 `MediaBackendType::FFmpeg`。

---

## 7. 已知限制

- 当前仅支持“按屏幕捕获”，未提供“窗口捕获/区域捕获”。
- DXGI 在部分场景（会话切换、显卡切换、权限/桌面状态变化）可能出现 duplication 丢失，当前实现会尝试重建，但仍建议补充更细错误码与 UI 提示。
- 目前路径为 CPU 映射 + swscale 转换，后续可优化为 GPU 路径降低 CPU 占用。
- 硬件编码依赖 FFmpeg 构建能力和驱动环境；若缺少 `h264_nvenc/h264_amf`，自动模式会回退到软件编码。
- 若系统 HDR 持续输出浮点帧，DXGI 仍会出现有效帧被间歇跳过的抖动；建议用户关闭 HDR 或切换 FFmpeg 后端。

---

## 8. 后续建议

- 增加设置项：屏幕后端手动切换（DXGI / FFmpeg / Qt）
- 增加窗口捕获、区域捕获
- 增加屏幕采集错误码映射与更清晰 UI 反馈
- 评估 GPU 直转 YUV 或共享纹理方案以进一步降耗
- 评估增加 `R16G16B16A16_FLOAT` 到 SDR 的色调映射转换，提升 HDR 场景下 DXGI 直接可用性

---

## 9. 回归建议清单

- 编译回归：`media_dxgi`、`runtime`、`service`、`widget`
- 功能回归：多显示器切换、启停、帧率切换、鼠标指针开关
- 稳定性回归：频繁切屏、长时间采集、最小化/恢复窗口
- 视觉回归：高 DPI 下分辨率显示、下拉宽度、工具栏对齐
