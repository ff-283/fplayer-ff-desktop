# 推拉流开发文档

## 1. 目标与范围（2026-04 更新）

本文档描述当前“推拉流”功能的工程落地方式，包括：

- 分层架构
- 关键接口
- UI 交互流程
- FFmpeg 库侧实现策略
- 已知限制与后续扩展建议

当前版本聚焦“可用闭环 + 可调优 + 分层可维护”，在进程内通过 **FFmpeg 动态库（libavformat / libavcodec / libavutil / libswscale / libswresample / libavdevice）** 实现推拉流，**不依赖**本机 `ffmpeg` 可执行文件或命令行。

---

## 2. 分层架构

## 2.1 API 层

文件：

- `api/include/fplayer/api/net/istream.h`
- `api/src/istream.cpp`

核心抽象：`fplayer::IStream`

主要接口：

- `startPush(inputUrl, outputUrl)`
- `startPull(inputUrl, outputUrl)`
- `stop()`
- `isRunning()`
- `lastError()`
- `recentLog()`
- `lastExitCode()`

---

## 2.2 后端实现层（FFmpeg）

文件：

- `backend/net_ffmpeg/include/fplayer/backend/net_ffmpeg/streamffmpeg.h`
- `backend/net_ffmpeg/src/streamffmpeg.cpp`
- `backend/net_ffmpeg/CMakeLists.txt`

实现类：`fplayer::StreamFFmpeg`

实现策略（当前）：

- 文件直推默认使用 **copy/remux**（低开销）
- 当配置了码率/尺寸/帧率时，文件模式切换为 **转码推流**（解码 -> 缩放 -> 编码 -> 推流）
- 屏幕模式支持两条视频链路：
  - `__screen_preview__`：消费 DXGI 预览帧总线（推荐，降低采集开销与鼠标闪烁）
  - `__screen_capture__`：FFmpeg `gdigrab` 采集链路（回退）
- 摄像头模式默认走“当前预览帧推流”，避免设备二次占用冲突
- 在 **独立工作线程** 中执行循环，避免阻塞 UI；`stop()` 通过 `AVFormatContext::interrupt_callback` 与原子标志请求中断
- 启动时调用 `avformat_network_init()`（仅一次）；输入侧在 `avformat_open_input` 前预分配 `AVFormatContext` 以便尽早挂上中断回调
- `recentLog` 为应用侧追加的简短状态/错误说明（带长度上限，避免无限增长）；`lastExitCode` 为 libav 错误码（成功为 `0`）

封装与编码策略：

- **推流**：输出封装为 FLV（RTMP/多数直播链路友好）
- **拉流**：由输出路径/协议自动推断容器
- **编码器**：优先 H.264，不可用时回退 MPEG4
- **实时参数**：支持 `fps / size / bitrate`，并提供“跟随当前”策略
- **音频来源参数**：推流侧提供 `audio source` 选项（屏幕模式可选，默认关闭）
- **屏幕音频编码**：`gdigrab` 链路下音频走 `decode + swresample + AAC encode + mux`，避免 FLV 对输入音频格式不兼容导致 `-22`
- **时间戳策略**：屏幕/转码路径优先使用源帧时间戳并做单调修正，减轻播放端“忽快忽慢”

说明：文件模式默认不重编码；其它模式按配置与场景走编码链路。

---

## 2.3 Runtime 层

文件：

- `runtime/include/fplayer/runtime/runtime.h`
- `runtime/src/runtime.cpp`
- `runtime/CMakeLists.txt`

新增：

- `createStream(MediaBackendType backend)`

当后端类型为 `FFmpeg` 时，返回 `StreamFFmpeg` 实例。

---

## 2.4 Service 层

文件：

- `service/include/fplayer/service/service.h`
- `service/src/service.cpp`

新增能力：

- 初始化：`initStream(...)`
- 控制：`streamStartPush(...)` / `streamStartPull(...)` / `streamStop()`
- 状态：`streamIsRunning()` / `streamLastError()`
- 日志：`streamRecentLog()` / `streamLastExitCode()`

Service 作为 UI 与后端之间的统一入口，避免 UI 直接触碰后端对象。

---

## 2.5 Widget/UI 层

文件：

- `widget/src/capturewindow.cpp`

新增入口：

- 顶部菜单 `推拉流`（与 `模式` 并列）
  - `推流`
  - `拉流`

新增交互（当前）：

- 推流配置常驻控制台（不再“点击确定即关闭”）
- 拉流配置对话框
- 协议模板（RTMP / RTSP / SRT）
- 尺寸下拉（含常见分辨率）+ 手动输入
- 宽高比保持开关
- 帧率、码率可调（支持“跟随当前”）
- 声音来源下拉（含“关闭声音”）
- 实时状态与日志显示
- 拉流“直接预览到播放窗口”模式

---

## 3. 关键流程

## 3.1 推流流程

1. 用户打开 `推流` 对话框
2. 配置输出地址与推流参数（fps/尺寸/码率/宽高比/声音来源）
3. 调用 `Service::streamStartPushByScene(...)`
4. Service 根据场景组装推流请求并转发到 `IStream::startPush(...)`
5. `StreamFFmpeg` 在后台线程按策略执行（copy / transcode / capture / preview-frame）
6. UI 定时拉取状态与日志展示（包含“最终生效参数”）

## 3.2 拉流流程（文件落地）

1. 用户打开 `拉流` 对话框
2. 配置输入流与输出文件
3. 调用 `Service::streamStartPull(...)`
4. `StreamFFmpeg` 在后台线程中拉流并写入目标文件（封装由输出路径推断）

## 3.3 拉流流程（直接预览）

1. 用户勾选 `直接预览到播放窗口`
2. 不走 `streamStartPull` 落盘流程
3. 直接调用现有播放器链路 `openMediaFile(inputUrl)` 进入播放预览

说明：该预览流程与“屏幕捕获模式”共用同一预览控件体系；当前 Windows 下屏幕采集默认后端为 `DXGI`。当 Service 检测到屏幕后端为 `Dxgi` 时，屏幕推流会自动切换到 `DXGI预览帧 -> FFmpeg编码推流` 链路。

---

## 4. CMake 与模块开关

文件：`backend/CMakeLists.txt`

- `FPLAYER_BUILD_NET_FFMPEG` 需显式确保为 `ON`（否则推流后端不可用）
- `backend/net_ffmpeg` 会被纳入构建

文件：`runtime/CMakeLists.txt`

- 启用 `FPLAYER_BUILD_NET_FFMPEG` 时链接 `FPlayer_Backend_Net_FFmpeg`

文件：`backend/net_ffmpeg/CMakeLists.txt`

- 与工程其余模块一致，通过 `cmake/3rd.cmake` 中的 `FFMPEG_HEADER`、`FFMPEG_LIBRARIES` 链接 **avdevice、avformat、avcodec、swscale、swresample、avutil**（与 `backend/media_ffmpeg` 使用同一套 FFmpeg 预编译包）

---

## 5. 设计取舍

## 5.1 为何在进程内使用 libav* 而非调用 ffmpeg 进程

- 与项目已集成的 FFmpeg 动态库 **统一依赖**，安装包无需再携带或查找 `ffmpeg.exe`
- 行为在应用内可控，便于与 **中断、错误码、后续扩展（编码参数、多任务）** 结合
- 仍可通过 `lastError` / `lastExitCode` 对照 libav 错误排查问题

## 5.2 为何拉流预览复用播放器链路

- 避免重复实现网络解复用与渲染路径
- 复用现有 `player` 预览能力，集成成本最低

---

## 6. 已知限制

- 推流输出侧当前固定以 FLV 创建输出上下文；若需 RTSP/SRT 专用封装策略仍需继续细化
- 文件转码推流当前以视频链路为主，音频直通/重编码策略可继续完善
- 屏幕模式虽优先走 DXGI 预览帧推流，但该路径当前仅处理视频；若需音频需走 `gdigrab` 链路
- `gdigrab` 路径下受系统桌面合成和显卡环境影响，极端场景仍可能波动
- 纯 **copy** 模式下，部分 **H.264/HEVC 在 MP4 与 FLV 之间** 可能需要 bitstream filter；若遇失败，需按具体码流增加 bitstream 处理或改为重编码
- 地址历史仅会话内保留，未做持久化
- 日志是“最近片段”，不是完整历史

---

## 7. 后续扩展建议

- 增加协议感知的参数模板：
  - RTMP/RTSP/SRT 分别使用更合理的容器与 mux 参数
- 增加推流高级参数：
  - 编码器、GOP、关键帧间隔、音频参数
- 增加任务并发管理：
  - 支持多任务并行推/拉流
- 地址历史持久化（配置文件）
- 增加标准错误码映射，便于 UI 给出更清晰提示
